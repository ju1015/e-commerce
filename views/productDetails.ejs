<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= product.name %> - MyShop</title>
  <style>
    body {
      background: #000;
      font-family: Arial, sans-serif;
      color: #fff;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .container {
      width: 350px;
      padding: 20px;
      background: #1c1c1c;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(255,255,255,0.1);
      text-align: center;
    }
    img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 15px;
    }
    h1 {
      font-size: 1.3rem;
      margin-bottom: 8px;
      color: #ffc107;
    }
    p {
      margin-bottom: 10px;
      color: #ccc;
      font-size: 0.9rem;
    }
    .price {
      font-size: 1.2rem;
      color: #28a745;
      margin-bottom: 15px;
    }
    .btn {
      padding: 8px 15px;
      background: #28a745;
      color: #fff;
      text-decoration: none;
      border: none;
      border-radius: 6px;
      font-size: 0.9rem;
      cursor: pointer;
      transition: background 0.3s;
      margin: 5px;
    }
    .btn:hover {
      background: #218838;
    }
    .back-btn {
      background: #6c757d;
    }
    .back-btn:hover {
      background: #5a6268;
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="/uploads/<%= product.image %>" alt="<%= product.name %>">
    <h1><%= product.name %></h1>
    <p><%= product.description %></p>
    <p class="price">$<%= product.price %></p>

    <!-- Add to Cart -->
    <form action="/add-to-cart/<%= product._id %>" method="POST" style="margin-bottom:10px;">
      <button class="btn" type="submit">Add to Cart</button>
    </form>

    <!-- Back to Shop Button -->
    <form action="/shopNow" method="POST" style="display:inline;">
        <button type="submit" class="btn back-btn">← Back to Shop</button>
    </form>

    </form>
  </div>
</body>
</html>
